import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
    boxTitle: {
        backgroundColor: '#fff',
        borderRadius: 10,
        padding: 50,
        fontSize: 20,
    },
    text: {
        fontSize: 30,
    }
})

export default styles